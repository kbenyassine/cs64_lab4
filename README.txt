1. The scret formula file is encrypting and decrypting a list of numbers. 
First, it encrypts the krabby array with a secret formula using eponents and modulo.
Then, it decrypts the krabby array and reverts the encrypted array back to the original numbers found in the krabby array.

2i. The numbers do not hold for the values because you get an integer overflow
2ii. The numbers do not hold for the values because you get an integer overflow.